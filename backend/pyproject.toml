
[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "peerring-backend"
version = "0.1.0"
description = "Spatial PeerRing - AI Tutoring Platform Backend"
authors = [
    {name = "PeerRing Team", email = "team@peerring.ai"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "websockets>=12.0",
    "pydantic>=2.5.0",
    "redis>=5.0.0",
    "langchain>=0.1.0",
    "langgraph>=0.0.40",
    "openai>=1.0.0",
    "anthropic>=0.7.0",
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "python-jose[cryptography]>=3.3.0",
    "python-multipart>=0.0.6",
    "httpx>=0.25.0",
    "prismtrace-sdk>=0.4.3"
]

[project.optional-dependencies]
dev = [
    "pytest-cov>=4.1.0",
    "black>=23.0.0",
    "flake8>=6.0.0",
    "mypy>=1.6.0",
    "pre-commit>=3.5.0"
]

[tool.black]
line-length = 88
target-version = ['py311']

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true